start
```bash
git clone git@github.com:<organization>/<this repository>.git
cd <this repository>
git submodule update --init --recursive
docker-compose up -d
```

stop
```bash
docker-compose down
```
